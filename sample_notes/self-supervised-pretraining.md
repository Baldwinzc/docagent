# Self-Supervised Pre-training

**Self-supervised pre-training** learns from unlabelled text by creating the
labels from the data itself (predict a masked or next token). It lets models learn
general language structure from huge corpora before any task-specific labels, and
is the foundation of the pretrain-then-adapt recipe.
