# Full Fine-Tuning

**Full fine-tuning** updates all of a pre-trained model's weights on a task's
labelled data. It usually reaches the best task accuracy but produces a separate
full-size model per task and is expensive to store and train at scale, which
motivates parameter-efficient alternatives.
