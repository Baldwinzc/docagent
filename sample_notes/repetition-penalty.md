# Repetition Penalty

A **repetition penalty** lowers the probability of tokens (or n-grams) already
generated, countering the loops and verbatim repetition that greedy and
low-temperature decoding tend to produce.
