# ReAct

**ReAct** interleaves reasoning and acting: the model thinks about what it needs,
calls a tool, observes the result, and repeats. Gathering evidence incrementally
lets it reformulate when a result is unhelpful instead of committing to one guess.
