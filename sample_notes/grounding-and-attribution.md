# Grounding and Attribution

**Grounding** means every claim in an answer is traceable to retrieved evidence,
and **attribution** attaches the specific source (file, page, line) to it. A robust
system verifies emitted citations against what was actually retrieved and drops
those that do not match, so citations can be trusted.
