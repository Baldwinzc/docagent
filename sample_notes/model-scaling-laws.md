# Scaling Laws

**Scaling laws** describe how loss falls predictably as a power law in model size,
dataset size, and compute. They guide how to allocate a compute budget — notably
that many models were undertrained, and that data and parameters should grow
together (compute-optimal training).
