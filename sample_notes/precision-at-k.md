# Precision@k

**Precision@k** measures how many of the top-k retrieved items are actually
relevant. Low precision wastes the generator's context window on noise; it trades
off against recall as k grows.
