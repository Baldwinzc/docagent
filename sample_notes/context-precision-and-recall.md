# Context Precision and Recall

In RAG evaluation, **context precision** is the fraction of retrieved chunks that
are relevant, and **context recall** is the fraction of needed information that was
retrieved. Separating them from answer quality shows whether a failure came from
retrieval or from generation.
