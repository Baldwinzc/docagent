# Reward Modeling

A **reward model** is trained on human preference comparisons to score how good a
response is. It turns sparse human judgments into a dense signal that an alignment
algorithm can optimize against; its quality caps how well RLHF can align the model.
