# Query Routing

**Query routing** sends a request down different paths based on its type — e.g. a
simple single-fact lookup to a direct retrieval+answer path, and a complex
multi-hop question to a multi-step agent. Routing keeps simple queries fast and
cheap while reserving heavy machinery for hard ones.
