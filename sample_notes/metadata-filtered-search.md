# Metadata-Filtered Search

Vector stores keep **metadata** (source, date, author, tags) beside each vector so
queries can restrict the search — e.g. only this collection or this date range.
Combining a metadata filter with vector similarity (pre- or post-filtering) keeps
results both relevant and in-scope.
