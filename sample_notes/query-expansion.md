# Query Expansion

**Query expansion** adds related terms or paraphrases to a query (from synonyms,
pseudo-relevance feedback, or an LLM) so it matches relevant documents that use
different wording. It mainly helps sparse retrieval, which otherwise misses
vocabulary mismatches.
