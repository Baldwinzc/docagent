# Mean Reciprocal Rank (MRR)

**MRR** averages `1/rank` of the first relevant result across queries, rewarding
systems that put a relevant item near the top. It cares only about the first hit,
so it suits tasks with a single right answer.
