# Multi-Agent Orchestration

**Multi-agent orchestration** assigns roles to several LLM calls — e.g. planner,
parallel researchers, verifier, synthesizer — coordinated by an explicit control
flow. It can improve coverage and reliability on complex tasks at the cost of more
model calls, so it is reserved for genuinely hard queries.
