# Agentic RAG

**Agentic RAG** puts retrieval inside an agent loop: the model searches, inspects
results, reformulates the query, searches again, and answers only with enough
evidence — or declines. Because it can re-query, retrieval quality largely
determines how well the loop performs.
