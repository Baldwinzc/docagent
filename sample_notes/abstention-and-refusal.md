# Abstention and Refusal

**Abstention** is correctly declining to answer when the corpus does not cover the
question, instead of fabricating. A relevance threshold on retrieval scores is a
principled trigger: if nothing clears it, say so. Refusal accuracy measures how
often the system abstains when it should.
