# Pruning

**Pruning** removes weights, neurons, heads, or layers that contribute little,
yielding a smaller/faster model. Unstructured pruning zeroes individual weights
(needs sparse kernels to pay off); structured pruning removes whole units and
speeds up dense hardware directly.
