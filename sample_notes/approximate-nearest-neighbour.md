# Approximate Nearest-Neighbour (ANN)

**ANN** search trades a little recall for a large speedup by not examining every
vector — using graphs, clustering, or quantization to look at a promising subset.
It is what makes vector search practical at hundreds of thousands of vectors and
beyond.
