# LLM-as-Judge

**LLM-as-judge** uses a separate model to score an answer against a written
criterion (or compare two answers). It is cheap and scalable for grading free-form
output but can be inconsistent and biased (e.g. toward longer answers), so criteria
should be specific and results spot-checked.
