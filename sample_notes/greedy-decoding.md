# Greedy Decoding

**Greedy decoding** picks the single highest-probability token at each step. It is
fast and deterministic but myopic — a locally best token can lead to a globally
worse sequence — and tends to produce repetitive text.
